# Cixibara 手册维护

本仓库只放 Cixibara 语录手册。「Cixibara」是基于数学老师陈晓（昵称「慈禧」）教学风格的 AI 人设手册，手册主要在 claude.ai Project / Gemini 里用来解题，这边负责编辑和版本管理。

## 两个文件
- `Cixibara.md` —— **S 版（人类版）**，是源头。只有前言、版权声明、语录正文；不含「AI 使用定位」「AI 解题须知」，也不含词条内的「AI 使用」注解。**标题带版本号** `# Cixibara va.b`（主.次两位，不含修订号）。
- `Cixibara-AI.md` —— **AI 版**，标题 `# Cixibara for AI`，不带版本号。比 S 版多「AI 使用定位」+「AI 解题须知」4 条：第 2、3 条按 AI 能力分两种情形（**能弹选择框的**弹「工具范围」四选一、高阶解法弹确认框；**不能弹框的**默认①基础档、不够用才升最低够用档并注明、高阶解法直接附上），并叮嘱克制、避免语气激进。语录词条与 S 版一字不差。

## 改动规则
- **改语录**（增/删/改词条、改正文）→ **两份一起改**，语录部分保持完全一致。
- 只改「AI 说明」（解题须知、使用定位）→ 只改 `Cixibara-AI.md`。
- **改语录时**：主动检查是否有"如果被领导看到会有不良影响"的夸张/敏感表述（如影射老师失职、贬低/侮辱学生、拿真实病症开玩笑等），发现了提出来给用户判断，不擅自改写语录内容。
- 不动用户已有的文件结构，不往手册里加历史段。
- 在 Code 里解题 → 读 `Cixibara-AI.md`。

## 版本与提交
- **版本历史只记在 git 提交信息里**，手册文件内不写历史。回看：`git log --oneline`。
- **提交信息格式（固定）**：`cixibara: v旧→v新 MMDD [description]`，方括号里的说明**用英文写**（语录原文、词条名照抄中文）。例：`cixibara: v5.38.2→v5.39.0 0710 [add entry 2-29 家徒一壁]`。
- 版本号三段式 `va.bc.d`（主.次.修订）：**大改** 次版本 +10、修订归零；**增加语录/新内容** 次版本 +1、修订归零；**bugfix / 纯结构调整** 修订 +1。主版本固定，除非用户要求升级。当前版本看最近一条提交。
- **S 版标题版本号**只在主/次版本变化时同步更新；纯修订号变化不改标题。

## 分支
- `claude/default` 是唯一主干。改文件前先 `git fetch origin claude/default`，基于最新版改。
- 会话分支改完提交 → 合并进 `claude/default` → 推送，之后忽略会话分支。
