# 会话分工

这个仓库支持多个 Code 会话各自负责不同的事。每个会话天生互相隔离（各自独立对话），
只有 `CLAUDE.md`（根目录，通用规则）是所有会话共享的。

**新会话第一条消息发一个关键词，就自动进入对应分工**（规则写在根目录 `CLAUDE.md` 里）：

## 会话列表

| 会话 | 用途 | 新会话第一条消息（就发这一个词） |
|------|------|------|
| 技术指点 | 聊 Code 系统、会话、git 等技术问题 | （就是这个会话，不用新建） |
| main | 日常主聊天，什么都能聊（含临时数学题，需要时让我读一下 `projects/math/` 即可） | `main` |
| cixibara | 陈晓老师语录手册的编辑与版本管理（现 v5.38，在 `语录/`；解题在 claude.ai Project） | `cixibara` |
| friends | 《老友记》学英语词汇/文化梗 | `friends` |

## 目录结构

```
CLAUDE.md                    ← 通用规则，所有会话共享
语录/                         ← Cixibara 手册（CLAUDE.md + Cixibara手册.md）
projects/
├── math/CLAUDE.md
└── friends/CLAUDE.md
```

---

*创建时间：2026-07-06*
